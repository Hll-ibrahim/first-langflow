import { fetch, FormData } from "undici";
import { Flow } from "./flow.js";
import type { LangflowClientOptions, Tweaks } from "./types.js";
export declare class LangflowClient {
    #private;
    baseUrl: string;
    basePath: string;
    langflowId?: string;
    apiKey?: string;
    fetch: typeof fetch;
    defaultHeaders: Headers;
    constructor(opts: LangflowClientOptions);
    flow(flowId: string, tweaks?: Tweaks): Flow;
    request(path: string, method: string, body: string | FormData, headers: Headers, query?: Record<string, string>): Promise<unknown>;
}
